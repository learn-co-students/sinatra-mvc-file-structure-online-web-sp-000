#This holds a list of all the gems needed to run the application. The bundler gem provides us access to a terminal command:
# bundle install. Bundler will look in the Gemfile and install any gems, as well as any gem dependencies for this application.

#Go ahead and enter this command in terminal. It will create a Gemfile.lock file for you, which is just a documentation of 
#the specific gems version that should be installed.
gem 'sinatra'
gem 'tux'
gem 'require_all'
gem 'rake'
gem 'thin'
gem 'shotgun'
gem 'pry'

group :test do
  gem 'rspec'
  gem 'rack-test'
end
